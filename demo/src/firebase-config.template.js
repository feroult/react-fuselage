import firebase from 'firebase';

var config = {
    apiKey: "???",
    authDomain: "???",
    databaseURL: "??",
    storageBucket: "??",
    messagingSenderId: "??"
};

firebase.initializeApp(config);
